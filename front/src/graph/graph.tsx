import React, { useState } from "react";
import init, { read_file } from "wasm-lib";

const graph = () => {
  const [data, setData] = useState();
  const [twoWatData, setTwoWayData] = useState();
  const [dataLevels, setDataLevels] = useState();
  return <div></div>;
};

export default graph;
